Comentários & Dúvidas:

- Spawn dentro de um monitor? Spawn dentro de um construtor de um monitor?

- Comandos 'signal' e 'broadcast': 'variable' ou 'expression' (permite functions calls)?

- O 'private' já não seria automático?
attribute-declaration
	: 'private' variable-declaration

- O use de 'self' dentro de um monitor
self.values = [Char](size)

- As funções que são private não usam o lock

- 'wait' statement
while stackIsFull() wait in stackIsFullQueue
wait in stackIsFullQueue until stackIsNotFull()

- ?s
	- 'for' na gramática